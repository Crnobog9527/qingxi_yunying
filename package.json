{
  "name": "qingxi-yunying",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "dev": "vercel dev",
    "serve": "python3 -m http.server 5173",
    "build": "rm -rf public && mkdir -p public && cp index.html public/ && cp -R src public/src",
    "preview": "vercel dev"
  },
  "dependencies": {
    "@vercel/blob": "^2.5.0"
  },
  "devDependencies": {
    "vercel": "^54.18.0"
  }
}
