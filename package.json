{
  "name": "qingxi-yunying",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "serve": "python3 -m http.server 5173",
    "build": "rm -rf public && mkdir -p public && cp index.html login.html favicon.svg public/ && cp -R src public/src",
    "preview": "echo \"Use: pnpm exec vercel dev\"",
    "db:migrate:neon": "node scripts/migrate-neon.js",
    "db:seed:neon": "node scripts/seed-neon.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@vercel/blob": "^2.5.0",
    "@vercel/functions": "^3.7.4"
  },
  "devDependencies": {
    "vercel": "^54.18.0"
  }
}
